# 프로비저닝 대상 호스트
# ANSIBLE_INVENTORY 또는 -i 로 다른 파일 지정 가능
all:
  children:
    ci_servers:
      hosts:
        localhost:
          ansible_connection: local
      vars:
        ansible_user: "{{ lookup('env', 'ANSIBLE_USER') | default('root', true) }}"
